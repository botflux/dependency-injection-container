/**
 * Used for defining service container metadata
 */
export const INJECT_TOKEN_METADATA = 'serviceContainerToken'

/**
 * Used for defining service container name
 */
export const SERVICE_TOKEN_METADATA = 'serviceContainerName'
